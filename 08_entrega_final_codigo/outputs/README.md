# Outputs pendientes

`eda/` contiene outputs IEEE-CIS de la etapa exploratoria. `experimentacion/`, `costos/` y `modelo_final/` se agregarán al terminar sus kernels privados en la cuenta Kaggle autorizada. No se incluyen métricas sintéticas ni resultados históricos como sustitutos. El estado actual está en `../run_manifest.json`.
