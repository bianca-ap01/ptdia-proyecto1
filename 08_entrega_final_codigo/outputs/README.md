# Outputs pendientes

Esta carpeta recibirá `eda/`, `experimentacion/`, `costos/` y `modelo_final/` tras ejecutar los cuatro notebooks con IEEE-CIS en la cuenta Kaggle autorizada. No se incluyen métricas sintéticas ni resultados históricos como sustitutos. El estado actual está en `../run_manifest.json`.
