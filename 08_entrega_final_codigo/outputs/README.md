# Outputs pendientes

`eda/`, `experimentacion/`, `costos/` y `modelo_final/` contienen outputs IEEE-CIS descargados de los kernels privados de la cuenta Kaggle autorizada. `python ../verify.py` concilia cortes, elección, capacidad, costos y gate de adaptación. No se incluyen métricas sintéticas ni resultados históricos como sustitutos. El estado está en `../run_manifest.json`.
